<h1>Delete: <%= ingredient.name %></h1>
<form method="POST" action="<%= "/ingredients/#{ingredient.id}" %>">
  <input type="hidden" name="_method" value="DELETE">
  <p>Are you sure you want to delete <%= ingredient.name %>?</p>
  <a href="<%= "/ingredients/#{ingredient.id}" %>" class="btn btn-outline-secondary">Cancel</a>
  <button type="submit" class="btn btn-danger">Delete</button>
</form>
